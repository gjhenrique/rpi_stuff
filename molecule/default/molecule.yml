---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: local
    image: archlinux:latest
    tmpfs:
      - /tmp
      - /run
    privileged: True
    port_bindings:
      - 9092: 9092
      - 9118: 9118
    published_ports:
      - 0.0.0.0:9092:9092/TCP
      - 0.0.0.0:9118:9118/TCP
    tmpfs:
      - /tmp
      - /run
    volumes:
      - "/sys/fs/cgroup:/sys/fs/cgroup:ro"
provisioner:
  name: ansible
  config_options:
    defaults:
      stdout_callback: yaml
      bin_ansible_callbacks: yes
verifier:
  name: ansible
