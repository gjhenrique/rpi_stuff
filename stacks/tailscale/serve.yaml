---
services:
  tailscale:
    ports:
      - ${TS_PORT}
    volumes:
      - ./serve.json:/tailscale-serve-raw.json:ro
    environment:
      # Processed by the entrypoint of the sidecar
      TS_SERVE_CONFIG: /var/lib/tailscale/serve.json
      TS_PORT: ${TS_PORT}
